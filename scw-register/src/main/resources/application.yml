server:
  port: 8761
spring:
  application:
    name: SCW-REGISTER
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false #��EurekaServer����ע�ᵽ������ע������
    fetch-registry: false       #���ڴ���������������ȡ��������Ϣ
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka
